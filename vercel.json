{
  "buildCommand": "cd mosana_flutter && flutter build web --release --web-renderer canvaskit",
  "outputDirectory": "mosana_flutter/build/web",
  "framework": null,
  "installCommand": "if [ ! -d \"flutter\" ]; then git clone https://github.com/flutter/flutter.git -b stable --depth 1; fi && flutter/bin/flutter config --enable-web && cd mosana_flutter && ../flutter/bin/flutter pub get",
  "devCommand": "cd mosana_flutter && flutter run -d web-server --web-hostname=0.0.0.0 --web-port=3000",
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        {
          "key": "Cross-Origin-Embedder-Policy",
          "value": "require-corp"
        },
        {
          "key": "Cross-Origin-Opener-Policy",
          "value": "same-origin"
        }
      ]
    }
  ],
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
