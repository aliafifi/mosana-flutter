name = "mosana-flutter"
compatibility_date = "2024-01-01"

# Build configuration
[build]
command = "bash build.sh"
cwd = "."
watch_dir = "mosana_flutter"

[build.upload]
format = "directory"
dir = "mosana_flutter/build/web"

# No environment variables needed - build.sh handles everything

# Routes
[[routes]]
pattern = "/*"
function = ""
